PC_NAME=redmine-sv

MYSQL_ROOT_PASSWORD=root_pass

# DB_ROOT_PASSWORD=root_pass
DB_NAME_WP=it_media_db
DB_USER_WP=wp_user
DB_PASSWORD_WP=wp_pass

DB_NAME_REDMINE=redmine_db
DB_USER_REDMINE=redmine_user
DB_PASSWORD_REDMINE=redmine_pass

# === TODO: 庁内LAN SMTPサーバの情報に書き換えること ===
SMTP_ADDRESS=mail.example.lg.jp    # TODO: 庁内SMTPサーバのホスト名
SMTP_PORT=25                       # TODO: ポート番号 (25 or 587)
SMTP_DOMAIN=example.lg.jp          # TODO: 庁内ドメイン名
SMTP_AUTH=login                    # TODO: 認証不要なら none に変更
SMTP_USER=redmine@example.lg.jp    # TODO: SMTPユーザ名 (認証不要なら空)
SMTP_PASSWORD=                     # TODO: SMTPパスワード (認証不要なら空)
SMTP_TLS=false                     # TODO: STARTTLS使用なら true
# =====================================================
